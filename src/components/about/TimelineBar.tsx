import { IconDefinition, faPython } from "@fortawesome/free-brands-svg-icons";
import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";

export default function TimelineBar() {
  return (
    <div
      className={`my-2 mx-auto w-2 h-48 bg-gradient-to-b from-transparent via-dark to-transparent`}
    ></div>
  );
}
